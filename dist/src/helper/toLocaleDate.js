const toLocaleDate = (date) => {
    const det = new Date(date);
    const localDateString = det.toLocaleDateString();
    return localDateString;
};
export default toLocaleDate;
//# sourceMappingURL=toLocaleDate.js.map